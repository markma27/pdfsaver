server {
    listen 80;
    server_name ocr.pdfsaver.com;

    client_max_body_size 200m;
    client_body_timeout 300s;

    location / {
        proxy_pass http://127.0.0.1:8123;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Increase timeouts for OCR processing
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }
}

# HTTPS configuration (uncomment and configure SSL certificates)
# server {
#     listen 443 ssl http2;
#     server_name ocr.pdfsaver.com;
#
#     ssl_certificate /etc/ssl/certs/pdfsaver.crt;
#     ssl_certificate_key /etc/ssl/private/pdfsaver.key;
#
#     client_max_body_size 200m;
#     client_body_timeout 300s;
#
#     location / {
#         proxy_pass http://127.0.0.1:8123;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         proxy_connect_timeout 300s;
#         proxy_send_timeout 300s;
#         proxy_read_timeout 300s;
#     }
# }

